# 免费 API 配置（与系统 B 同名变量，各自独立 .env，不共享密钥文件）
# 复制本文件为 .env 并填入自己的值。
# 默认使用系统 B 同款免费模型 GLM-4-Flash（智谱 AI）。

AI_API_KEY=你的免费API密钥
AI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
AI_MODEL=glm-4-flash

# 可选：HTTP/HTTPS 代理（无代理留空）
AI_PROXY=
