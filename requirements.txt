# Web 框架
Flask>=3.0.0
Werkzeug>=3.0.0

# 数据库（SQLAlchemy + 多租户迁移）
Flask-SQLAlchemy>=3.1.0
SQLAlchemy>=2.0.0
Flask-Migrate>=4.0.0
alembic>=1.13.0

# 鉴权（注册/登录/密码哈希/会话）
Flask-Login>=0.6.0
# 密码哈希
bcrypt>=4.1.0
# 表单 + CSRF 防护
Flask-WTF>=1.2.0
email_validator>=2.1.0

# 配置 / 环境变量
PyYAML>=6.0
python-dotenv>=1.0.0

# HTTP（AI 调用，OpenAI 兼容接口）
requests>=2.31.0

# 加密（用户自带 Token 的 Fernet 加解密）
cryptography>=42.0.0

# 课件解析
python-pptx>=0.6.23
python-docx>=1.1.0
pdfplumber>=0.11.0

# 存档导出（Excel / PDF）
openpyxl>=3.1.0
reportlab>=4.0.0
