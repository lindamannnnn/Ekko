# 课评系统 · 腾讯云部署教程

面向第一次买服务器的人写的，每一步都能照着点。

---

## 先看这个：一件必须提前知道的事

你选了「买域名」，那么有个绕不开的规则：

> **域名要绑定中国大陆的服务器，必须先完成 ICP 备案，否则 80 端口会被运营商直接拦截。**

备案是走流程，**7-20 天**，期间网站不能用域名访问。所以整个上线拆成两个阶段：

| 阶段 | 何时 | 访问方式 | 能做什么 |
|---|---|---|---|
| **阶段一** | 今天，1 小时内 | `http://公网IP` | 系统完全可用，自己和同事先内测 |
| **阶段二** | 备案通过后，10 分钟 | `https://你的域名` | 正式对外，加密传输 |

**不用等备案才开始**。今天就能把系统跑起来，备案是并行进行的。

<details>
<summary>不想等备案？还有一个选择（点开看权衡）</summary>

买**中国香港**地域的服务器，域名免备案，当天就能用 HTTPS。

代价：
- 国内用户访问延迟从 20ms 涨到 60-100ms，页面加载略慢
- 香港轻量服务器带宽通常只有 30Mbps（国内是 200Mbps+）
- 价格略贵

**如果你的用户都在国内（教培机构基本都是），我建议还是选国内地域 + 老实备案**，长期体验好很多。备案期间用 IP 访问完全不影响你先用起来。
</details>

---

## 花销与时间预算

| 项目 | 费用 | 说明 |
|---|---|---|
| 轻量应用服务器 2核2G | **¥60-120 / 年** | 新用户特惠价，务必用新账号买 |
| 域名 .com | **¥60-80 / 首年** | 续费约 ¥90/年 |
| HTTPS 证书 | **¥0** | Let's Encrypt 免费，脚本自动续期 |
| **合计** | **约 ¥130-200 / 年** | |

时间：今天动手 **1 小时**能跑起来，备案另算。

---

## 第 1 步 · 买服务器

### 1.1 进入购买页

打开 https://curl.qcloud.com/lightweight ，或在腾讯云控制台搜「**轻量应用服务器**」。

> 为什么是轻量而不是 CVM？轻量便宜、带宽大、控制台简单、自带防火墙界面。你这个场景用轻量完全够，CVM 是给复杂架构用的。

### 1.2 关键选项照着填

| 选项 | 选什么 | 为什么 |
|---|---|---|
| **地域** | 广州 / 上海 / 北京（挑离你近的） | 国内用户访问快。**选定后不能改**，想清楚 |
| **镜像** | 应用镜像 → **Docker CE** | 已经装好 Docker，省一步。找不到就选「系统镜像 → Ubuntu 24.04 LTS」，脚本会自己装 |
| **实例套餐** | **2核 2G 4M带宽** | 你确认的配置 |
| **系统盘** | 40G+ SSD | 默认即可 |
| **购买时长** | 1 年 | 按年买单价最低 |

### 1.3 设置密码

创建时会让你设 root 密码，或者让你**先创建后重置**。

密码要求：12 位以上，含大小写字母+数字+符号。

> **务必现在就把密码记到密码管理器里**。等下连服务器要用。

### 1.4 记下公网 IP

创建完成后，在实例列表能看到「公网 IP」，形如 `123.207.xx.xx`。

**把它抄下来**，下面每一步都要用。本教程里我统一写作 `你的IP`。

---

## 第 2 步 · 买域名

### 2.1 购买

腾讯云控制台 → 搜「**域名注册**」 → 输入你想要的名字查询。

建议：
- 优先 `.com`，其次 `.cn`
- 名字短、好记、和业务相关，比如 `kepingxitong.com`
- **避开** `.top` `.xyz` 这类后缀，备案通过率低，有些还被邮箱当垃圾域名

### 2.2 实名认证

买完**立刻做实名认证**（控制台 → 域名管理 → 实名认证）。

- 不实名 3 天后域名会被暂停解析
- 实名不通过**不能备案**
- 审核 1-3 天

### 2.3 提交备案

腾讯云控制台 → 搜「**网站备案**」→ 按引导填。

需要准备：
- 身份证正反面照片
- 手持身份证照片（小程序里拍，有引导）
- 服务器购买信息（自动带出）
- 网站名称：**别写「课评系统」这种可能被当作新闻/教育资质类的名字**，写成「XX工作室」「XX信息技术」这类更容易过

流程：腾讯云初审(1-2天) → 工信部短信核验(当天) → 管局审核(3-20天)

> 备案期间服务器照常用，只是不能用域名访问。**继续往下做，不用等。**

---

## 第 3 步 · 连上服务器

### 方式 A：网页直连（最简单，推荐第一次用）

腾讯云控制台 → 轻量应用服务器 → 你的实例 → 右上角「**登录**」按钮。

浏览器里直接开出一个黑色终端窗口，已经是 root 身份。**不用装任何软件**。

### 方式 B：本地终端 SSH（后续常用，更顺手）

在你自己电脑上打开 PowerShell 或 Git Bash：

```bash
ssh root@你的IP
```

第一次连接会问 `Are you sure you want to continue connecting?`，输入 `yes` 回车，然后输入密码。

> 输密码时屏幕**完全没有反应**（不显示星号），这是 Linux 的正常行为，盲打完直接回车。

---

## 第 4 步 · 一键部署

连上服务器后，**复制粘贴下面这三行**，回车：

```bash
curl -fsSL https://raw.githubusercontent.com/lindamannnnn/Ekko/main/deploy/setup-server.sh -o /tmp/setup.sh \
  || curl -fsSL https://ghfast.top/https://raw.githubusercontent.com/lindamannnnn/Ekko/main/deploy/setup-server.sh -o /tmp/setup.sh
bash /tmp/setup.sh
```

脚本会自动做完这些事：

1. 体检系统，内存不够自动加 2G swap（防止构建时卡死）
2. 装 Docker（如果买的是 Docker CE 镜像会自动跳过）
3. **配置腾讯云内网镜像加速** ← 关键，不配的话拉 Python 镜像会超时失败
4. 拉取代码（GitHub 连不上会自动切镜像站）
5. **生成全部随机密钥**（SECRET_KEY、后台路径、网关密钥、管理员密码）
6. 构建并启动服务
7. 健康检查确认真的起来了

中途只会问你两个问题：

| 提问 | 怎么答 |
|---|---|
| `请粘贴 AI_API_KEY` | 你的大模型密钥。没有的话去 https://open.bigmodel.cn 注册，创建 API Key，免费额度够用很久。**留空也能继续，之后改 `.env` 补上** |
| `管理员登录邮箱` | 直接回车用默认，或填你的邮箱 |

首次构建要 **3-8 分钟**（在下载安装依赖），耐心等。

### ⚠️ 部署完立刻做这件事

```bash
cat /opt/ekko/CREDENTIALS.txt
```

会输出：

```
管理员邮箱  ： admin@local.dev
管理员密码  ： xK9mP2qR7nW4         ← 随机生成的
后台入口路径： /a3f9c2e1b8/          ← 随机生成的
后台网关密钥： 7d4e...（32位）        ← 随机生成的
```

**马上抄到密码管理器**。后台路径是随机的，丢了就只能重新看这个文件。

> 为什么后台路径要随机？因为公网上有大量扫描器专门扫 `/admin`。随机路径 + 网关密钥是两道门，扫描器根本找不到入口。

---

## 第 5 步 · 让外网能访问

现在服务只在服务器内部跑着，外面还进不来。两件事：

### 5.1 装 nginx 反代

```bash
bash /opt/ekko/deploy/setup-nginx.sh
```

十几秒完成。它做了：反向代理到 5000 端口、放宽上传大小到 64M（课件 PPT 大）、放宽超时到 300 秒（AI 生成慢，不然会 504）。

### 5.2 放行端口 ← 最容易忘的一步

回到**腾讯云控制台** → 你的实例 → 「**防火墙**」标签 → 添加规则：

| 应用类型 | 端口 | 来源 |
|---|---|---|
| HTTP | 80 | 0.0.0.0/0 |
| HTTPS | 443 | 0.0.0.0/0 |

> 443 现在用不上，但一起加了，省得备案通过后再来一趟。
>
> **注意：轻量服务器的防火墙在腾讯云控制台，不是服务器里的 ufw。** 只在服务器里开是没用的。

### 5.3 试试看

浏览器打开：

```
http://你的IP
```

应该能看到登录页了。用 `CREDENTIALS.txt` 里的邮箱密码登录。

后台入口是：`http://你的IP/你的随机路径/`

---

## 第 6 步 · 备案通过后上 HTTPS

收到备案通过短信后，做这两步。

### 6.1 域名解析

腾讯云控制台 → 域名管理 → 你的域名 → 「解析」：

| 记录类型 | 主机记录 | 记录值 |
|---|---|---|
| A | `@` | 你的IP |
| A | `www` | 你的IP |

10 分钟左右生效。验证：

```bash
ping 你的域名.com
```

看到的 IP 是你的服务器 IP 就对了。

### 6.2 一条命令开 HTTPS

```bash
bash /opt/ekko/deploy/enable-https.sh 你的域名.com 你的邮箱@example.com
```

脚本会：检查解析是否正确 → 申请 Let's Encrypt 免费证书 → 自动改 nginx 配置 → 强制 HTTP 跳 HTTPS → 加安全响应头 → 配置到期自动续期。

完成后访问 `https://你的域名.com`，地址栏出现小锁 🔒。

证书 90 天有效，**自动续期，不用管**。

---

## 日常运维

### 常用命令

```bash
cd /opt/ekko

docker compose logs -f          # 实时看日志（Ctrl+C 退出）
docker compose logs --tail 100  # 看最近 100 行
docker compose restart          # 重启
docker compose ps               # 看运行状态
```

### 更新代码

你在本地改完推到 GitHub 后，服务器上：

```bash
cd /opt/ekko
git pull
docker compose up -d --build
```

数据不会丢（数据库挂载在宿主机 `instance/` 目录）。

### 备份 —— 这个真的要做

SQLite 是**单文件数据库**，服务器故障或误删就全没了。

先手动跑一次确认能用：

```bash
bash /opt/ekko/deploy/backup.sh
```

然后设成每天凌晨 3 点自动备份：

```bash
crontab -e
```

在打开的编辑器里添加一行（按 `i` 进入编辑，粘贴，按 `Esc`，输入 `:wq` 回车保存）：

```
0 3 * * * bash /opt/ekko/deploy/backup.sh >> /var/log/ekko-backup.log 2>&1
```

备份存在 `/opt/ekko-backups/`，自动保留 30 天。

> **更保险的做法**：定期把备份拉回自己电脑，服务器整机挂了才有救。
> ```bash
> scp root@你的IP:/opt/ekko-backups/ekko_*.tar.gz ./
> ```

### 改配置

```bash
cd /opt/ekko
nano .env              # 改完 Ctrl+O 保存，Ctrl+X 退出
docker compose up -d   # 让改动生效
```

---

## 出问题了看这里

### 网站打不开

按顺序排查：

```bash
# 1. 容器在跑吗？
docker compose ps
# 应该看到 State = Up (healthy)

# 2. 服务内部正常吗？
curl http://127.0.0.1:5000/health
# 应该返回 {"status":"ok"}

# 3. nginx 正常吗？
systemctl status nginx
curl http://127.0.0.1

# 4. 都正常但外网还是不通 → 99% 是防火墙
#    去腾讯云控制台确认 80 端口已放行
```

### 容器起不来

```bash
docker compose logs --tail 50
```

常见原因：

| 日志关键词 | 原因 | 解决 |
|---|---|---|
| `ModuleNotFoundError` | 依赖没装全 | `docker compose build --no-cache` 重建 |
| `Address already in use` | 5000 端口被占 | `lsof -i:5000` 找到并停掉 |
| `Killed` / 构建中断 | 内存不够 | 确认 swap 开了：`free -h` |
| `no such table` | 数据库没初始化 | 重启一次即可，代码启动时会自动建表 |

### AI 生成课评失败

```bash
# 看具体报错
docker compose logs --tail 50 | grep -i "error\|ai"
```

| 现象 | 原因 |
|---|---|
| `401 Unauthorized` | API Key 错了，检查 `.env` 里的 `AI_API_KEY` |
| `ProxyError` | `.env` 里 `AI_PROXY` 有值，清空它 |
| `timeout` | 大模型响应慢，正常现象，重试即可 |
| `余额不足` | 去智谱控制台充值或换模型 |

### 忘了后台地址

```bash
cat /opt/ekko/CREDENTIALS.txt
# 或
grep ADMIN_PATH /opt/ekko/.env
```

### 磁盘满了

```bash
df -h                        # 看占用
docker system prune -a       # 清理旧镜像，能释放几个 G
```

---

## 安全清单

部署完对照检查：

- [x] SECRET_KEY 是随机值 —— 脚本已自动生成
- [x] 后台路径随机化 —— 脚本已自动生成
- [x] 后台网关密钥 —— 脚本已自动生成
- [x] 容器只监听 127.0.0.1，不裸奔公网 —— compose 已配置
- [x] `.env` 权限 600，不在 git 里 —— 已处理
- [ ] **改掉管理员默认密码**（登录后到个人设置改）
- [ ] **上 HTTPS**（备案后执行 enable-https.sh）
- [ ] **配置自动备份**（crontab）
- [ ] 腾讯云控制台开启「登录告警」，有人登录服务器会发短信

---

## 现在能扛多少人

当前配置（2核2G + gunicorn 4 worker × 4 线程 + SQLite）：

| 指标 | 能力 |
|---|---|
| 同时在线浏览 | 50-100 人没问题 |
| 同时点「生成课评」 | 16 个并发（超出会排队等待，不会报错） |
| 数据量 | 几万条课评轻松 |

**什么时候需要升级：**

- 老师反馈「点生成要等很久」 → 升 4核4G，worker 调到 8
- 出现 `database is locked` 报错 → 该换 Postgres 了
- 用户超过 200 人 → 建议 Postgres + 对象存储

到那一步再说，现在够用。

---

## 附：文件位置速查

| 内容 | 路径 |
|---|---|
| 项目代码 | `/opt/ekko` |
| 配置文件 | `/opt/ekko/.env` |
| **登录凭据** | `/opt/ekko/CREDENTIALS.txt` |
| 数据库 | `/opt/ekko/instance/app.db` |
| 上传文件 | `/opt/ekko/uploads/` |
| 备份 | `/opt/ekko-backups/` |
| nginx 配置 | `/etc/nginx/sites-available/ekko` |
| nginx 日志 | `/var/log/nginx/ekko.*.log` |
