# 密钥与环境（绝不入库）
.env
*.env
.env.*
!.env.example

# 运行时产物
instance/
uploads/
__pycache__/
*.pyc
*.pyo

# 旧单体残留（不允许提交）
src/web/
src/oj/
rules/
start.bat
tools/
output/
data/
references/
123/

# 平台无关
.venv/
venv/
.venv-doc/   # 上一轮误提交进仓库的废弃 venv，已全删，避免复发

# 临时与历史目录（本地保留用于参考/待人工清理，不入库）
logs_*.txt
_removed_orphan_*/
_旧git历史_*/
*quantifying*/   # 含鬼目录 ' quantifying/'（首 commit 误提交）
*.lnk

# 调试与一次性临时脚本（本地排查用，不入库）
_dbg/
_dbg_run.log*
_*_tmp.py
_proxy_backup_*.json

# 服务器生成的凭据文件（含管理员密码，严禁入库）
CREDENTIALS.txt

# 笔记/草稿
classreview.html
# 已被忽略：data/ references/ tools/ skills/ 见上半段旧单体残留
